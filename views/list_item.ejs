<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body>
    <div>
        <div class="dayHeader">
            <div class="playerTag">
                <img src="Soccerball.jpeg" alt="Temp Icon" class="roundcorners playerImage">
                <div class="playerName"><%= name %></div>
            </div>
            <div class="statContainer">
                <% if(type == 'keeper'){ %>
                    <button class="statButton">Sv</button>
                <%} %>
                <button class="statButton" onclick="statHandler('goal', '<%= team %>')">G</button>
                <button class="statButton">A</button>
                <button class="statButton">OG</button>
            </div>
            <%# data[i][listTitle] %>                    
        </div>
    </div>
</body>
</html>